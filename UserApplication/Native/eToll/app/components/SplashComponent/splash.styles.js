import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  splashContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  image: {
    flex:1,
  },
});
